/**
 * Storage service exports
 */

export { StorageService, storageService } from './StorageService';
export type {
  Location,
  FavoriteLocation,
  UserPreferences,
  StorageServiceConfig,
  StorageError,
} from '../../types/storage';
export { StorageKeys, StorageErrorType } from '../../types/storage';
